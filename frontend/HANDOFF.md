# Session Handoff — Frontend

## Current State

Frontend work has not started yet. The project is scaffolded (Vite + React + TypeScript, TanStack Router/Query, shadcn/ui) but no application screens have been built.

Backend is complete through **Phase 6** (project closure + record locking). Phase 6.5 (required documents, expected/placeholder entities) is planned but not yet implemented — check `backend/data/HANDOFF.md` for the current backend state before assuming what APIs are available.

## Before Starting Frontend Work

1. Read `ROADMAP.md` fully — it documents non-obvious API behaviors not visible in the OpenAPI schema
2. Check `backend/data/HANDOFF.md` for the current backend phase and any deferred design decisions
3. Set up the OpenAPI client (see `CLAUDE.md` for the generate command)

## Phase Plan

See `ROADMAP.md` for the full phase breakdown.
